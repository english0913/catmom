# 현재 코드 분석 (catmom / prototype.html)

## 1) 프로젝트 개요
- 이 저장소는 단일 HTML 파일(`prototype.html`)에 **UI/CSS/게임 로직(JS)** 이 함께 들어있는 캔버스 기반 고양이 시뮬레이터 프로토타입입니다.
- `Patch21` 기준으로 다음 이슈를 다룹니다: 빈그릇 선택 버그 수정, 놀이/스트레스 밸런스 튜닝, 화장실/배설 시스템, 시체 전환 및 청소, 잔해 오브젝트 추가.

## 2) 코드 구조
- **설정/밸런스 계층**: `CONFIG`에 이동/욕구/배설/구토/사망/관계치 등 거의 모든 상수를 집중 관리.
- **도메인 타입 계층**: `Mode`, `Actions`, `Sub`, `ItemType` enum-like 객체로 상태 전이를 명시.
- **상태 계층**:
  - 전역: `cats`, `items`, 모드 토글(`treatMode`, `feedMode`, `sandMode`, `cleanMode`), 로그 배열.
  - 개체: 고양이(`needs/personality/relationships/status/ai`)와 아이템(`food/sand/occupantId/deadCatId`)에 책임이 분리.
- **시뮬레이션 계층**:
  - `tick()`이 관계, 배설/오라, 욕구 감소, 결정, 이동/행동 업데이트를 프레임마다 수행.
  - `makeDecision()`이 생존 우선순위(배고픔/저에너지) → 사회/놀이/휴식으로 의사결정.
- **렌더/입력 계층**:
  - `draw()`와 개별 `drawXxx()` 함수들로 아이템/고양이/선택 UI 표시.
  - 캔버스 마우스 이벤트로 관찰/아이템/청소/사료/모래/간식 모드 동작 처리.

## 3) 핵심 동작 요약
### 3-1. 고양이 AI
- 의사결정은 욕구(`satiety/energy/fun/stress`)와 쿨다운에 따라 행동(EAT/SLEEP/PLAY/SOCIAL/REST/WANDER/HIDE)을 선택.
- 스트레스가 매우 높으면 휴식 우선, 저에너지/저포만은 생존 행동 우선으로 설계.

### 3-2. 음식/구토
- 밥그릇 선택은 거리/잔량/혼잡/도둑질 성향을 점수화해서 고릅니다.
- 빈그릇만 있을 때 `null`을 반환하도록 hotfix가 들어가 있어, 과거 "빈그릇인데 EAT 시도" 루프를 방지.
- 과식 구토는 임계치/확률/쿨다운으로 제어되어 연속 구토를 완화.

### 3-3. 화장실/배설/청소
- 일정 주기마다 배설 욕구를 체크하고, 화장실 모래 상태에 따라 정상 사용 vs 실수를 판정.
- 배설물/토사물/잔해는 주변 스트레스 오라를 발생시켜 청소의 게임플레이 가치를 부여.
- 청소 모드에서 `DEAD_BODY` 제거 시 연결된 고양이 엔티티도 완전 삭제해 데이터 참조를 정리.

### 3-4. 사망 처리
- 에너지가 임계치 아래로 일정 시간 지속되면 고양이를 `dead + removed` 처리하고 시체 아이템으로 변환.
- 기존 고양이 본체는 시뮬레이션 루프에서 제외되며, 시체는 청소 가능 오브젝트로 남김.

## 4) 장점
- **설정 중심 설계**: 밸런스 파라미터를 `CONFIG`에 모아 빠른 튜닝 가능.
- **행동/상태 분리**: `ai.phase`(MOVE/DO)와 `action`을 분리해 로직 추적이 쉬움.
- **실사용 UX 고려**: 모드 버튼, 속도 슬라이더, 선택 패널, 로그/부정 이벤트 분리 등 디버깅 친화적.

## 5) 리스크 및 개선 포인트
1. **단일 파일 집중도**
   - UI/상태/시뮬레이션/렌더가 1파일에 모여 있어 기능 확장 시 충돌 위험이 큼.
   - 권장: `config.js`, `ai.js`, `render.js`, `ui.js` 등으로 분리.

2. **전역 가변 상태 의존성**
   - `cats/items/mode`를 많은 함수가 직접 참조/수정해 회귀 추적이 어려움.
   - 권장: 상태 변경 함수(예: reducer/command 패턴) 도입.

3. **성능 스케일링 한계**
   - 관계/오라/충돌 검사에서 다중 중첩 루프가 많아 개체 수 증가 시 O(N²)에 근접.
   - 권장: 공간 분할(그리드/쿼드트리) 또는 근접 후보 캐시.

4. **테스트 부재**
   - 브라우저 런타임 로직이지만 자동 회귀 테스트가 없어 패치 안정성 확인이 어려움.
   - 권장: 핵심 순수 함수(`chooseBowlForCat`, `maybeLitterNeed` 판정부 등) 분리 후 단위 테스트.

## 6) 결론
- 현재 코드는 **프로토타입 단계에서 기능 밀도가 높고, Patch21의 목표(버그 수정 + 시스템 확장)** 를 잘 반영하고 있습니다.
- 다음 단계는 기능 추가보다 **모듈화 + 상태관리 정리 + 테스트 도입**이 유지보수 효율을 크게 높일 구간입니다.
